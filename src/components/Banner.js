import { Box } from '@mui/material';

const Banner = () => {
    return (
        <Box sx={{ backgroundColor: 'grey.200', width: '20%', textAlign: 'center', display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
            <p>Banner Quảng Cáo</p>
        </Box>
    );
};

export default Banner;
